

# an example
# """
class pycompute(object):
    def add(self, a, b):
        return a + b

    def subtraction(self, a, b):
        return a - b

    def compute(self, a, b):
        print("a = %.2f" % a)
        print("b = %.2f" % b)
        # c = self.add(a, b)
        c = self.subtraction(a, b)
        return c
# """
